---
# Source: nautobot/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "nautobot-test-connection"
  labels:
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['nautobot:8080']
  restartPolicy: Never
