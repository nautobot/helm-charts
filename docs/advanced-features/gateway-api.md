# Kubernetes Gateway API

The support for Kubernetes Gateway API HTTP routes resources is in beta stage. This will change heavily in the future releases.
Please check the `values.yaml` and the relevant `gateway-api.yaml` template to better understand what is supported right now.

This documentation page will be updated as soon as we are in a more "finalized" stage.
