# Gateway API

The support for Gateway API HTTP routes resources is in beta stage. This will change heavily in the future realeases.
Please check the `values.yaml` and the relevant `gateway-api.yaml` template to better understand what is supported right now.

This documentation page will be updated as soon as we are in a more "finalised" stage.
